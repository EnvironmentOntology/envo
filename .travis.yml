sudo: required

services:
  - docker

before_install:
  - docker pull obolibrary/odkfull
   
   
# command to run tests
script: docker run -m 4g -v $PWD:/work -w /work --rm -ti obolibrary/odkfull /bin/bash -c "cd src/envo &&  make travis_test"

#after_success:
#  coveralls

# whitelist
branches:
  only:
    - master

notifications:
  email:
    - cjmungall@lbl.gov

